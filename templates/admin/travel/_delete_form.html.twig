<form method="post" action="{{ path('admin_travel_delete', {'id': travel.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
	<input type="hidden" name="_method" value="DELETE">
	<input type="hidden" name="_token" value="{{ csrf_token('delete' ~ travel.id) }}">
	<button class="btn btn-danger btn-sm">delete</button></a>
</form>
